pipeline { 
    agent any 
        stages { 
        
           stage('Build') {
            steps {
                sh 'make'
                archiveArtifacts artifacts: '/target/window.jar', fingerprint: true 
                }
            }
            
        }
    }
