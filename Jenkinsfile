pipeline { 
    agent any 
    environment{
        NEW_VERSION='2.279'
    }
        stages { 
        
           stage('Build') {
            steps {
                echo "Building application.." 
                echo "Building version:${NEW_VERSION}"
                }
            }
            
        }
    }
